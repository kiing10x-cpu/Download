# Core (required — bot won't start without these)
python-telegram-bot[job-queue]>=21.7,<22
yt-dlp>=2024.8.6

# Optional (bot runs fine without these — features auto-disable gracefully)
qrcode[pil]        # local branded payment QR codes
matplotlib          # needed for 📊 /exportpdf charts
reportlab            # needed for 📊 /exportpdf
cryptography         # encrypted database backups
pymongo               # only used if you set MONGO_URI
imageio-ffmpeg          # bundles ffmpeg if it's not installed on the host
